import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "vertical-horizontal-scale-money-models",
  title: "Vertical, Horizontal Scale & Money Models",
  Component: App,
});
