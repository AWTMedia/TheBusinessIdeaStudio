import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "offer-pricing-economics",
  title: "Offer & Pricing Economics Calculator",
  Component: App,
});
