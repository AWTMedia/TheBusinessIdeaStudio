import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "client-onboarding-handoff-kit",
  title: "Client Onboarding & Handoff Kit — Matrix Blueprint",
  Component: App,
});
