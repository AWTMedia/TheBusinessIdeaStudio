import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "content-production-system-platform-understanding",
  title: "Content Production System — Platform Understanding",
  Component: App,
});
