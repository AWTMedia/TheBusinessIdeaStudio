import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "vertical-scale-matrix",
  title: "Content Production & Vertical Business Scale",
  Component: App,
});
