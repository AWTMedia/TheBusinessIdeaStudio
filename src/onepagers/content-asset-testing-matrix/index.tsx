import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "content-asset-testing-matrix",
  title: "Content Asset & Testing Matrix",
  Component: App,
});
