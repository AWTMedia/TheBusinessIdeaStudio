import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "target-market-template",
  title: "Target Market Identification — Template",
  Component: App,
});
