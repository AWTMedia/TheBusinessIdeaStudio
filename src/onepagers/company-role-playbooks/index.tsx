import App from "./App";
import { createLegacyOnePager } from "../legacy";

export default createLegacyOnePager({
  slug: "company-role-playbooks",
  title: "Company Role Playbooks — One Pager",
  Component: App,
});
